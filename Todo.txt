handle
	File test/dir2/TEST is a directory while file test/dir3/TEST is a regular empty file

synch files and directories

open ftl, which directory?

diff options compatibility with -q
	-b will not ignore the difference

delete not deleted file
	keep it colored differently

